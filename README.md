# primecursos (clean export)

This branch contains only the prime-cursos application files (course implementation) and minimal docs for deployment.

- Location: prime-cursos/
- Use courses.json in that folder or configure PRIME_DATA_DIR in inc_helpers.php to use an external data directory.

