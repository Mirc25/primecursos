const fs=require('fs');const path=require('path');const p=path.join(__dirname,'..','admin.fixed2.html');const s=fs.readFileSync(p,'utf8');const m=s.match(/<script[^>]*>([\s\S]*?)<\/script>/i); if(!m){console.error('no script');process.exit(2);} const code=m[1]; const lines=code.split(/\n/); for(let i=0;i<lines.length;i++){ console.log((i+1).toString().padStart(3,' ')+': '+lines[i]); }